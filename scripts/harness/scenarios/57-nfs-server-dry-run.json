{
  "id": "nfs-server-dry-run",
  "title": "NFS server - export path dry run",
  "destructive": false,
  "planSource": { "kind": "capability-selection", "capabilityIds": ["nfs-server"] },
  "expected": {
    "planType": "rebuild",
    "items": [{ "id": "capability:nfs-server", "capabilityKey": "fs.share.nfs", "supportLevel": "full-migration", "minActions": 5 }],
    "conflictsBlock": 0,
    "conflictsWarn": 0,
    "approvalsRequired": [{ "kind": "data-strategy-confirm" }],
    "remainingRisks": ["Export paths", "Network ACLs"]
  },
  "approvalAcks": { "risksAck": "ALL", "approvalsAck": "ALL" },
  "verify": { "actionRunStatuses": ["succeeded"], "auditChecks": ["exportfs -s", "client subnet review", "export path review"] },
  "notes": ["NFS exports are config-managed; backing datasets and client scopes require explicit review."]
}
