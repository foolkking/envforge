{
  "id": "terraform-iac-dry-run",
  "title": "Terraform IaC - credentials and state dry run",
  "destructive": false,
  "planSource": { "kind": "capability-selection", "capabilityIds": ["terraform-iac"] },
  "expected": {
    "planType": "rebuild",
    "items": [{ "id": "capability:terraform-iac", "capabilityKey": "developer.terraform", "supportLevel": "full-migration", "minActions": 3 }],
    "conflictsBlock": 0,
    "conflictsWarn": 0,
    "approvalsRequired": [{ "kind": "secret-confirm" }],
    "remainingRisks": ["Terraform state"]
  },
  "approvalAcks": { "risksAck": "ALL", "approvalsAck": "ALL" },
  "verify": { "actionRunStatuses": ["succeeded"], "auditChecks": ["terraform version", "credentials redaction", "state migration blocked"] },
  "notes": ["Terraform CLI config is governed; terraform.tfstate files are explicitly blocked from automatic migration."]
}
