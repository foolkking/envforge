{
  "id": "swap-config-dry-run",
  "title": "Swap configuration - fstab and zram dry run",
  "destructive": false,
  "planSource": { "kind": "capability-selection", "capabilityIds": ["swap-config"] },
  "expected": {
    "planType": "rebuild",
    "items": [{ "id": "capability:swap-config", "capabilityKey": "system.swap", "supportLevel": "full-migration", "minActions": 3 }],
    "conflictsBlock": 0,
    "conflictsWarn": 0,
    "approvalsRequired": [{ "kind": "data-strategy-confirm" }],
    "remainingRisks": ["Cloud images", "Existing zram"]
  },
  "approvalAcks": { "risksAck": "ALL", "approvalsAck": "ALL" },
  "verify": { "actionRunStatuses": ["succeeded"], "auditChecks": ["swapon --show", "fstab rollback", "zram review"] },
  "notes": ["Swap configuration is full-migration only with filesystem and zram compatibility review."]
}
