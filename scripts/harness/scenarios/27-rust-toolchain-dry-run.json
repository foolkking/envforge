{
  "id": "rust-toolchain-dry-run",
  "title": "Rust toolchain - Cargo config dry run",
  "destructive": false,
  "planSource": { "kind": "capability-selection", "capabilityIds": ["rust-toolchain"] },
  "expected": {
    "planType": "rebuild",
    "items": [{ "id": "capability:rust-toolchain", "capabilityKey": "runtime.rust", "supportLevel": "full-migration", "minActions": 4 }],
    "conflictsBlock": 0,
    "conflictsWarn": 0,
    "approvalsRequired": [{ "kind": "secret-confirm" }],
    "remainingRisks": ["Cargo packages"]
  },
  "approvalAcks": { "risksAck": "ALL", "approvalsAck": "ALL" },
  "verify": { "actionRunStatuses": ["succeeded"], "auditChecks": ["rustc --version", "cargo --version", "Cargo token redaction"] },
  "notes": ["Cargo registries and credentials are reviewed; target directories are rebuilt."]
}
