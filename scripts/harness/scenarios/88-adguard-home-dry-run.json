{
  "id": "adguard-home-dry-run",
  "title": "AdGuard Home - DNS cutover dry run",
  "destructive": false,
  "planSource": { "kind": "capability-selection", "capabilityIds": ["adguard-home"] },
  "expected": {
    "planType": "rebuild",
    "items": [{ "id": "capability:adguard-home", "capabilityKey": "app.dns.adguard-home", "supportLevel": "full-migration", "minActions": 3 }],
    "conflictsBlock": 0,
    "conflictsWarn": 0,
    "approvalsRequired": [{ "kind": "manual-dns-confirm" }, { "kind": "secret-confirm" }],
    "remainingRisks": ["UDP/TCP 53", "User hashes"]
  },
  "approvalAcks": { "risksAck": "ALL", "approvalsAck": "ALL" },
  "verify": { "actionRunStatuses": ["succeeded"], "auditChecks": ["AdGuard status", "DNS port 53", "DoH/DoT certs"] },
  "notes": ["AdGuard Home migration requires DNS resolver conflict resolution and credential review."]
}
