{
  "id": "dotnet-runtime-dry-run",
  "title": ".NET runtime - NuGet config dry run",
  "destructive": false,
  "planSource": { "kind": "capability-selection", "capabilityIds": ["dotnet-runtime"] },
  "expected": {
    "planType": "rebuild",
    "items": [{ "id": "capability:dotnet-runtime", "capabilityKey": "runtime.dotnet", "supportLevel": "full-migration", "minActions": 3 }],
    "conflictsBlock": 0,
    "conflictsWarn": 0,
    "approvalsRequired": [{ "kind": "secret-confirm" }],
    "remainingRisks": ["NuGet package cache"]
  },
  "approvalAcks": { "risksAck": "ALL", "approvalsAck": "ALL" },
  "verify": { "actionRunStatuses": ["succeeded"], "auditChecks": ["dotnet --version", "NuGet credential redaction"] },
  "notes": ["NuGet feeds are reviewed; package caches and build outputs are rebuilt."]
}
