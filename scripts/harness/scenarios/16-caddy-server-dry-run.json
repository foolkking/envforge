{
  "id": "caddy-server-dry-run",
  "title": "Caddy server - ACME storage and upstream dry run",
  "destructive": false,
  "planSource": {
    "kind": "capability-selection",
    "capabilityIds": ["caddy-server"]
  },
  "expected": {
    "planType": "rebuild",
    "items": [
      {
        "id": "capability:caddy-server",
        "capabilityKey": "web-server.caddy",
        "supportLevel": "full-migration",
        "minActions": 6
      }
    ],
    "conflictsBlock": 0,
    "conflictsWarn": 0,
    "approvalsRequired": [
      { "kind": "secret-confirm" },
      { "kind": "manual-dns-confirm" }
    ],
    "remainingRisks": [
      "ACME storage under /var/lib/caddy holds private keys"
    ]
  },
  "approvalAcks": {
    "risksAck": "ALL",
    "approvalsAck": "ALL"
  },
  "verify": {
    "actionRunStatuses": ["succeeded"],
    "auditChecks": [
      "caddy validate --config /etc/caddy/Caddyfile",
      "ACME storage secret confirmation",
      "DNS/domain routing confirmation",
      "site root and upstream review"
    ]
  },
  "notes": [
    "Caddy automatic HTTPS is certified through dry-run plan shape only; live issuance still requires DNS and port readiness."
  ]
}
