{
  "id": "mongodb-dry-run",
  "title": "MongoDB - dump restore dry run",
  "destructive": false,
  "planSource": { "kind": "capability-selection", "capabilityIds": ["mongodb"] },
  "expected": {
    "planType": "rebuild",
    "items": [{ "id": "capability:mongodb", "capabilityKey": "database.mongodb", "supportLevel": "full-migration", "minActions": 3 }],
    "conflictsBlock": 0,
    "conflictsWarn": 0,
    "approvalsRequired": [{ "kind": "data-strategy-confirm" }, { "kind": "secret-confirm" }],
    "remainingRisks": ["mongodump/mongorestore", "Replica-set"]
  },
  "approvalAcks": { "risksAck": "ALL", "approvalsAck": "ALL" },
  "verify": { "actionRunStatuses": ["succeeded"], "auditChecks": ["db.runCommand", "mongodump strategy", "keyFile auth review"] },
  "notes": ["MongoDB data uses logical backup/restore or replica-set resync; raw dbPath copy is not allowed."]
}
