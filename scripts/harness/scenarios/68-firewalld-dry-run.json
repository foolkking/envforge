{
  "id": "firewalld-dry-run",
  "title": "firewalld - SSH lockout dry run",
  "destructive": false,
  "planSource": { "kind": "capability-selection", "capabilityIds": ["firewalld"] },
  "expected": {
    "planType": "rebuild",
    "items": [{ "id": "capability:firewalld", "capabilityKey": "security.firewall.firewalld", "supportLevel": "full-migration", "minActions": 3 }],
    "conflictsBlock": 0,
    "conflictsWarn": 0,
    "approvalsRequired": [{ "kind": "firewall-lockout-confirm" }],
    "remainingRisks": ["Mutually exclusive with UFW", "Safe-apply"]
  },
  "approvalAcks": { "risksAck": "ALL", "approvalsAck": "ALL" },
  "verify": { "actionRunStatuses": ["succeeded"], "auditChecks": ["firewall-cmd --check-config", "SSH lockout", "rollback timer"] },
  "notes": ["firewalld requires SSH reachability confirmation before live apply."]
}
