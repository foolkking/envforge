{
  "id": "wireguard-vpn-dry-run",
  "title": "WireGuard VPN - peer key and routing dry run",
  "destructive": false,
  "planSource": { "kind": "capability-selection", "capabilityIds": ["wireguard-vpn"] },
  "expected": {
    "planType": "rebuild",
    "items": [{ "id": "capability:wireguard-vpn", "capabilityKey": "network.vpn.wireguard", "supportLevel": "full-migration", "minActions": 3 }],
    "conflictsBlock": 0,
    "conflictsWarn": 0,
    "approvalsRequired": [{ "kind": "secret-confirm" }],
    "remainingRisks": ["Per-peer private keys", "Peer endpoints"]
  },
  "approvalAcks": { "risksAck": "ALL", "approvalsAck": "ALL" },
  "verify": { "actionRunStatuses": ["succeeded"], "auditChecks": ["wg show", "peer key review", "AllowedIPs review"] },
  "notes": ["WireGuard key transport and routing policy are reviewed manually."]
}
