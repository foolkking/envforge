{
  "id": "gitea-server-dry-run",
  "title": "Gitea - dump restore dry run",
  "destructive": false,
  "planSource": { "kind": "capability-selection", "capabilityIds": ["gitea-server"] },
  "expected": {
    "planType": "rebuild",
    "items": [{ "id": "capability:gitea-server", "capabilityKey": "developer.gitea", "supportLevel": "full-migration", "minActions": 3 }],
    "conflictsBlock": 0,
    "conflictsWarn": 0,
    "approvalsRequired": [{ "kind": "backup-restore-confirm" }, { "kind": "secret-confirm" }],
    "remainingRisks": ["Repository git objects", "OAuth provider secrets"]
  },
  "approvalAcks": { "risksAck": "ALL", "approvalsAck": "ALL" },
  "verify": { "actionRunStatuses": ["succeeded"], "auditChecks": ["gitea --version", "gitea dump", "app.ini secrets"] },
  "notes": ["Gitea repositories and LFS objects move through gitea dump/restore."]
}
