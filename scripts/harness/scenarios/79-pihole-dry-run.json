{
  "id": "pihole-dry-run",
  "title": "Pi-hole - DNS cutover dry run",
  "destructive": false,
  "planSource": { "kind": "capability-selection", "capabilityIds": ["pihole"] },
  "expected": {
    "planType": "rebuild",
    "items": [{ "id": "capability:pihole", "capabilityKey": "app.dns.pihole", "supportLevel": "full-migration", "minActions": 3 }],
    "conflictsBlock": 0,
    "conflictsWarn": 0,
    "approvalsRequired": [{ "kind": "manual-dns-confirm" }, { "kind": "secret-confirm" }],
    "remainingRisks": ["UDP/TCP 53", "admin password hashes"]
  },
  "approvalAcks": { "risksAck": "ALL", "approvalsAck": "ALL" },
  "verify": { "actionRunStatuses": ["succeeded"], "auditChecks": ["Pi-hole admin", "DNS port 53", "gravity data"] },
  "notes": ["Pi-hole migration requires DNS resolver conflict resolution and admin secret review."]
}
