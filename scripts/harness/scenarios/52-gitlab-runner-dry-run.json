{
  "id": "gitlab-runner-dry-run",
  "title": "GitLab Runner - token and executor dry run",
  "destructive": false,
  "planSource": { "kind": "capability-selection", "capabilityIds": ["gitlab-runner"] },
  "expected": {
    "planType": "rebuild",
    "items": [{ "id": "capability:gitlab-runner", "capabilityKey": "ci.gitlab-runner", "supportLevel": "full-migration", "minActions": 3 }],
    "conflictsBlock": 0,
    "conflictsWarn": 0,
    "approvalsRequired": [{ "kind": "secret-confirm" }],
    "remainingRisks": ["Runner registration tokens", "Custom executor"]
  },
  "approvalAcks": { "risksAck": "ALL", "approvalsAck": "ALL" },
  "verify": { "actionRunStatuses": ["succeeded"], "auditChecks": ["gitlab-runner verify", "runner token review", "executor dependency review"] },
  "notes": ["Runner re-registration is preferred; job caches and executor-specific data are not migrated automatically."]
}
