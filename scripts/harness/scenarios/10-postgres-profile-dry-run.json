{
  "id": "postgres-profile-dry-run",
  "title": "PostgreSQL profile - logical dump/restore dry run",
  "destructive": false,
  "planSource": {
    "kind": "capability-selection",
    "capabilityIds": ["postgres-profile"]
  },
  "expected": {
    "planType": "rebuild",
    "items": [
      {
        "id": "capability:postgres-profile",
        "capabilityKey": "database.postgresql",
        "supportLevel": "full-migration",
        "minActions": 7
      }
    ],
    "conflictsBlock": 0,
    "conflictsWarn": 0,
    "approvalsRequired": [
      { "kind": "data-strategy-confirm" }
    ],
    "remainingRisks": [
      "Roles, databases, extensions"
    ]
  },
  "approvalAcks": {
    "risksAck": "ALL",
    "approvalsAck": "ALL"
  },
  "verify": {
    "actionRunStatuses": ["succeeded"],
    "auditChecks": [
      "pg_dump / pg_dumpall manual strategy",
      "psql select 1 validation",
      "no raw rsync of /var/lib/postgresql",
      "data-strategy-confirm approval"
    ]
  },
  "notes": [
    "PostgreSQL data is migrated through explicit logical backup/restore review; raw filesystem copy is forbidden by the data strategy report."
  ]
}
