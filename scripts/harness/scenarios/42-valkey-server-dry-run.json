{
  "id": "valkey-server-dry-run",
  "title": "Valkey - RDB/AOF strategy dry run",
  "destructive": false,
  "planSource": { "kind": "capability-selection", "capabilityIds": ["valkey-server"] },
  "expected": {
    "planType": "rebuild",
    "items": [{ "id": "capability:valkey-server", "capabilityKey": "cache.valkey", "supportLevel": "full-migration", "minActions": 4 }],
    "conflictsBlock": 0,
    "conflictsWarn": 0,
    "approvalsRequired": [{ "kind": "data-strategy-confirm" }, { "kind": "secret-confirm" }],
    "remainingRisks": ["Persistence files"]
  },
  "approvalAcks": { "risksAck": "ALL", "approvalsAck": "ALL" },
  "verify": { "actionRunStatuses": ["succeeded"], "auditChecks": ["valkey-cli ping", "RDB/AOF review", "raw /var/lib/valkey copy refused"] },
  "notes": ["Valkey is Redis-compatible; persistence and ACL/password secrets require explicit review."]
}
