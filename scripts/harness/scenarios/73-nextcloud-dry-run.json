{
  "id": "nextcloud-dry-run",
  "title": "Nextcloud - maintenance backup dry run",
  "destructive": false,
  "planSource": { "kind": "capability-selection", "capabilityIds": ["nextcloud"] },
  "expected": {
    "planType": "rebuild",
    "items": [{ "id": "capability:nextcloud", "capabilityKey": "app.nextcloud", "supportLevel": "full-migration", "minActions": 3 }],
    "conflictsBlock": 0,
    "conflictsWarn": 0,
    "approvalsRequired": [{ "kind": "backup-restore-confirm" }, { "kind": "secret-confirm" }],
    "remainingRisks": ["Snap-installed nextcloud", "config.php"]
  },
  "approvalAcks": { "risksAck": "ALL", "approvalsAck": "ALL" },
  "verify": { "actionRunStatuses": ["succeeded"], "auditChecks": ["nextcloud.occ status", "maintenance mode", "config.php secrets"] },
  "notes": ["Nextcloud requires DB dump/restore, data directory transfer, and occ validation."]
}
