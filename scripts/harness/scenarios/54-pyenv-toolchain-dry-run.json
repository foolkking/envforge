{
  "id": "pyenv-toolchain-dry-run",
  "title": "pyenv Python version manager - rebuild scope dry run",
  "destructive": false,
  "planSource": { "kind": "capability-selection", "capabilityIds": ["pyenv-toolchain"] },
  "expected": {
    "planType": "rebuild",
    "items": [{ "id": "capability:pyenv-toolchain", "capabilityKey": "runtime.python.pyenv", "supportLevel": "full-migration", "minActions": 3 }],
    "conflictsBlock": 0,
    "conflictsWarn": 0,
    "remainingRisks": ["Compiling Python versions", "Compiled versions under ~/.pyenv/versions"]
  },
  "approvalAcks": { "risksAck": "ALL", "approvalsAck": "ALL" },
  "verify": { "actionRunStatuses": ["succeeded"], "auditChecks": ["pyenv --version", "Python version files", "virtualenv rebuild scope"] },
  "notes": ["pyenv versions and virtualenvs are rebuilt or explicitly selected; build dependencies vary by distribution."]
}
