{
  "id": "netdata-monitoring-dry-run",
  "title": "Netdata - cloud claim and stream dry run",
  "destructive": false,
  "planSource": { "kind": "capability-selection", "capabilityIds": ["netdata-monitoring"] },
  "expected": {
    "planType": "rebuild",
    "items": [{ "id": "capability:netdata-monitoring", "capabilityKey": "observability.metrics.netdata", "supportLevel": "full-migration", "minActions": 3 }],
    "conflictsBlock": 0,
    "conflictsWarn": 0,
    "approvalsRequired": [{ "kind": "secret-confirm" }],
    "remainingRisks": ["Cloud claim tokens", "Metric history"]
  },
  "approvalAcks": { "risksAck": "ALL", "approvalsAck": "ALL" },
  "verify": { "actionRunStatuses": ["succeeded"], "auditChecks": ["netdata service", "claim token review", "metric history rebuild"] },
  "notes": ["Netdata cloud/stream credentials are reviewed; local metric history is rebuilt unless explicitly exported."]
}
