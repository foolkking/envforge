{
  "id": "build-nginx-success",
  "title": "Build nginx — happy path",
  "destructive": false,
  "planSource": {
    "kind": "capability-selection",
    "capabilityIds": ["nginx-web-service"]
  },
  "expected": {
    "planType": "rebuild",
    "items": [
      {
        "id": "capability:nginx-web-service",
        "capabilityKey": "web-server.nginx",
        "supportLevel": "full-migration",
        "minActions": 3
      }
    ],
    "conflictsBlock": 0,
    "conflictsWarn": 0,
    "approvalsRequired": [],
    "remainingRisks": [
      "TLS private key paths must be reviewed before migrate."
    ]
  },
  "approvalAcks": {
    "risksAck": "ALL"
  },
  "verify": {
    "actionRunStatuses": ["succeeded"],
    "managedCapabilityKeys": ["web-server.nginx"]
  },
  "notes": [
    "nginx-web-service has one auditRemainingRisk (TLS private key paths). The harness fully acks it; the apply gate clears.",
    "After this scenario, scenario 5 (remove-managed-nginx) can run."
  ]
}
