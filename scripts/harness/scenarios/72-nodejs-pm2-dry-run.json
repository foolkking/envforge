{
  "id": "nodejs-pm2-dry-run",
  "title": "PM2 - saved process list dry run",
  "destructive": false,
  "planSource": { "kind": "capability-selection", "capabilityIds": ["nodejs-pm2"] },
  "expected": {
    "planType": "rebuild",
    "items": [{ "id": "capability:nodejs-pm2", "capabilityKey": "runtime.nodejs.pm2", "supportLevel": "full-migration", "minActions": 3 }],
    "conflictsBlock": 0,
    "conflictsWarn": 0,
    "approvalsRequired": [{ "kind": "secret-confirm" }],
    "remainingRisks": ["PM2 saved process list", "node-runtime-profile"]
  },
  "approvalAcks": { "risksAck": "ALL", "approvalsAck": "ALL" },
  "verify": { "actionRunStatuses": ["succeeded"], "auditChecks": ["pm2 --version", "dump.pm2 review", "environment redaction"] },
  "notes": ["PM2 migration is per user and does not own application directories."]
}
