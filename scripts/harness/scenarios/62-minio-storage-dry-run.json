{
  "id": "minio-storage-dry-run",
  "title": "MinIO - bucket replication dry run",
  "destructive": false,
  "planSource": { "kind": "capability-selection", "capabilityIds": ["minio-storage"] },
  "expected": {
    "planType": "rebuild",
    "items": [{ "id": "capability:minio-storage", "capabilityKey": "storage.object.minio", "supportLevel": "full-migration", "minActions": 3 }],
    "conflictsBlock": 0,
    "conflictsWarn": 0,
    "approvalsRequired": [{ "kind": "data-strategy-confirm" }, { "kind": "secret-confirm" }],
    "remainingRisks": ["Bucket data", "MINIO_ROOT_USER"]
  },
  "approvalAcks": { "risksAck": "ALL", "approvalsAck": "ALL" },
  "verify": { "actionRunStatuses": ["succeeded"], "auditChecks": ["minio health", "mc mirror strategy", "root credential review"] },
  "notes": ["Bucket data is moved by mc mirror, replication, or vendor tooling; credentials are re-issued or explicitly approved."]
}
