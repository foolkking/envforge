{
  "id": "memcached-dry-run",
  "title": "Memcached - bind address review dry run",
  "destructive": false,
  "planSource": { "kind": "capability-selection", "capabilityIds": ["memcached"] },
  "expected": {
    "planType": "rebuild",
    "items": [{ "id": "capability:memcached", "capabilityKey": "cache.memcached", "supportLevel": "full-migration", "minActions": 3 }],
    "conflictsBlock": 0,
    "conflictsWarn": 0,
    "remainingRisks": ["in-memory only"]
  },
  "approvalAcks": { "risksAck": "ALL", "approvalsAck": "ALL" },
  "verify": { "actionRunStatuses": ["succeeded"], "auditChecks": ["memcached -h", "bind address review", "no persistent cache data"] },
  "notes": ["Memcached has no persistent data; bind address and memory caps are reviewed before exposure."]
}
