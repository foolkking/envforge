{
  "id": "vault-secrets-dry-run",
  "title": "HashiCorp Vault - snapshot and unseal dry run",
  "destructive": false,
  "planSource": { "kind": "capability-selection", "capabilityIds": ["vault-secrets"] },
  "expected": {
    "planType": "rebuild",
    "items": [{ "id": "capability:vault-secrets", "capabilityKey": "security.secrets.vault", "supportLevel": "full-migration", "minActions": 3 }],
    "conflictsBlock": 0,
    "conflictsWarn": 0,
    "approvalsRequired": [{ "kind": "secret-confirm" }, { "kind": "backup-restore-confirm" }],
    "remainingRisks": ["Vault data (storage backend)", "Unseal keys"]
  },
  "approvalAcks": { "risksAck": "ALL", "approvalsAck": "ALL" },
  "verify": { "actionRunStatuses": ["succeeded"], "auditChecks": ["vault status", "snapshot/restore", "unseal key review"] },
  "notes": ["Vault state migration uses Vault APIs; unseal material stays out of band."]
}
