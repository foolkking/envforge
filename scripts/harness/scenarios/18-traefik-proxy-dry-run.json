{
  "id": "traefik-proxy-dry-run",
  "title": "Traefik proxy - ACME and provider dry run",
  "destructive": false,
  "planSource": {
    "kind": "capability-selection",
    "capabilityIds": ["traefik-proxy"]
  },
  "expected": {
    "planType": "rebuild",
    "items": [
      {
        "id": "capability:traefik-proxy",
        "capabilityKey": "network.reverse-proxy.traefik",
        "supportLevel": "full-migration",
        "minActions": 5
      }
    ],
    "conflictsBlock": 0,
    "conflictsWarn": 0,
    "approvalsRequired": [
      { "kind": "secret-confirm" },
      { "kind": "manual-dns-confirm" }
    ],
    "remainingRisks": [
      "acme.json holds private TLS keys"
    ]
  },
  "approvalAcks": {
    "risksAck": "ALL",
    "approvalsAck": "ALL"
  },
  "verify": {
    "actionRunStatuses": ["succeeded"],
    "auditChecks": [
      "traefik healthcheck",
      "acme.json secret confirmation",
      "Docker/file provider review",
      "dashboard exposure review"
    ]
  },
  "notes": [
    "Traefik provider state is reviewed explicitly; Docker provider usage depends on docker-host-profile target readiness."
  ]
}
