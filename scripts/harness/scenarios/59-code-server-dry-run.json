{
  "id": "code-server-dry-run",
  "title": "code-server - remote dev exposure dry run",
  "destructive": false,
  "planSource": { "kind": "capability-selection", "capabilityIds": ["code-server"] },
  "expected": {
    "planType": "rebuild",
    "items": [{ "id": "capability:code-server", "capabilityKey": "developer.code-server", "supportLevel": "full-migration", "minActions": 3 }],
    "conflictsBlock": 0,
    "conflictsWarn": 0,
    "approvalsRequired": [{ "kind": "secret-confirm" }],
    "remainingRisks": ["code-server config", "Workspace repositories"]
  },
  "approvalAcks": { "risksAck": "ALL", "approvalsAck": "ALL" },
  "verify": { "actionRunStatuses": ["succeeded"], "auditChecks": ["code-server --version", "auth mode review", "workspace scope excluded"] },
  "notes": ["Auth, bind address, TLS, and reverse-proxy exposure are reviewed before the service is reachable."]
}
