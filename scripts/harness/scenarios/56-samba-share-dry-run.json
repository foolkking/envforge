{
  "id": "samba-share-dry-run",
  "title": "Samba - share ACL and passdb dry run",
  "destructive": false,
  "planSource": { "kind": "capability-selection", "capabilityIds": ["samba-share"] },
  "expected": {
    "planType": "rebuild",
    "items": [{ "id": "capability:samba-share", "capabilityKey": "fs.share.samba", "supportLevel": "full-migration", "minActions": 3 }],
    "conflictsBlock": 0,
    "conflictsWarn": 0,
    "approvalsRequired": [{ "kind": "data-strategy-confirm" }, { "kind": "secret-confirm" }],
    "remainingRisks": ["Samba user accounts", "Share filesystem paths"]
  },
  "approvalAcks": { "risksAck": "ALL", "approvalsAck": "ALL" },
  "verify": { "actionRunStatuses": ["succeeded"], "auditChecks": ["testparm -s", "smbpasswd review", "share ACL review"] },
  "notes": ["Samba config is migratable; share contents, account database, and ACLs remain operator-reviewed."]
}
