{
  "id": "zabbix-monitoring-dry-run",
  "title": "Zabbix agent - endpoint and TLS PSK dry run",
  "destructive": false,
  "planSource": { "kind": "capability-selection", "capabilityIds": ["zabbix-monitoring"] },
  "expected": {
    "planType": "rebuild",
    "items": [{ "id": "capability:zabbix-monitoring", "capabilityKey": "observability.metrics.zabbix", "supportLevel": "full-migration", "minActions": 3 }],
    "conflictsBlock": 0,
    "conflictsWarn": 0,
    "approvalsRequired": [{ "kind": "secret-confirm" }],
    "remainingRisks": ["TLS PSK keys", "Server / ServerActive"]
  },
  "approvalAcks": { "risksAck": "ALL", "approvalsAck": "ALL" },
  "verify": { "actionRunStatuses": ["succeeded"], "auditChecks": ["zabbix agent validation", "TLS PSK review", "UserParameter script review"] },
  "notes": ["Zabbix agent endpoint and TLS PSK settings are environment-specific and require review."]
}
