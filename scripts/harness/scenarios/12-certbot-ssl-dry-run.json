{
  "id": "certbot-ssl-dry-run",
  "title": "Certbot SSL - manual ACME and key handling dry run",
  "destructive": false,
  "planSource": {
    "kind": "capability-selection",
    "capabilityIds": ["certbot-ssl"]
  },
  "expected": {
    "planType": "rebuild",
    "items": [
      {
        "id": "capability:certbot-ssl",
        "capabilityKey": "security.tls.certbot",
        "supportLevel": "full-migration",
        "minActions": 6
      }
    ],
    "conflictsBlock": 0,
    "conflictsWarn": 0,
    "approvalsRequired": [
      { "kind": "secret-confirm" },
      { "kind": "manual-dns-confirm" }
    ],
    "remainingRisks": [
      "Certificate private keys require explicit operator confirmation"
    ]
  },
  "approvalAcks": {
    "risksAck": "ALL",
    "approvalsAck": "ALL"
  },
  "verify": {
    "actionRunStatuses": ["succeeded"],
    "auditChecks": [
      "certbot certificates validation",
      "manual DNS/domain ownership confirmation",
      "private key secret confirmation",
      "dry-run does not contact ACME"
    ]
  },
  "notes": [
    "The dry-run validates plan shape only. Live ACME issuance requires operator-confirmed DNS or HTTP-01 reachability."
  ]
}
