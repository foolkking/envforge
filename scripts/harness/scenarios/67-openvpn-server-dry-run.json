{
  "id": "openvpn-server-dry-run",
  "title": "OpenVPN server - PKI and route dry run",
  "destructive": false,
  "planSource": { "kind": "capability-selection", "capabilityIds": ["openvpn-server"] },
  "expected": {
    "planType": "rebuild",
    "items": [{ "id": "capability:openvpn-server", "capabilityKey": "network.vpn.openvpn", "supportLevel": "full-migration", "minActions": 3 }],
    "conflictsBlock": 0,
    "conflictsWarn": 0,
    "approvalsRequired": [{ "kind": "secret-confirm" }],
    "remainingRisks": ["PKI material", "Pushed routes"]
  },
  "approvalAcks": { "risksAck": "ALL", "approvalsAck": "ALL" },
  "verify": { "actionRunStatuses": ["succeeded"], "auditChecks": ["openvpn --version", "PKI handling", "client-config-dir review"] },
  "notes": ["OpenVPN PKI and pushed-route changes are operator-reviewed."]
}
