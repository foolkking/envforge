{
  "id": "rsync-tools-dry-run",
  "title": "rsync tools - backup config dry run",
  "destructive": false,
  "planSource": { "kind": "capability-selection", "capabilityIds": ["rsync-tools"] },
  "expected": {
    "planType": "rebuild",
    "items": [{ "id": "capability:rsync-tools", "capabilityKey": "ops.backup.rsync", "supportLevel": "full-migration", "minActions": 6 }],
    "conflictsBlock": 0,
    "conflictsWarn": 0,
    "approvalsRequired": [{ "kind": "secret-confirm" }],
    "remainingRisks": ["Backup repositories"]
  },
  "approvalAcks": { "risksAck": "ALL", "approvalsAck": "ALL" },
  "verify": { "actionRunStatuses": ["succeeded"], "auditChecks": ["rsync --version", "daemon secrets review", "backup repository exclusion"] },
  "notes": ["Backup tools are installed and configured; backup datasets and remotes remain operator-owned."]
}
