# Nginx configuration validation failure

Models `nginx -t` failing before reload. The expected diagnostic recommends reviewing the diff, correcting certificate/include evidence through a reviewed Plan, and keeping rollback claims conservative. No target action is executed.
